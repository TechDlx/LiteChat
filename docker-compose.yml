services:
  litechat:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DATA_DIR: /data
    volumes:
      - litechat-data:/data
    restart: unless-stopped

volumes:
  litechat-data:
